s = set()

for i in range(8):

    a = input("enter the number ")

    s.add(a)

# Set is a data structure that stores unique elements. It automatically removes duplicates, so if the user enters the same number multiple times, it will only be stored once in the set. 

print(" Only Unique numbers from the input : ",s)